       import React from 'react';       import { render, fireEvent, waitFor } from '@testing-library/react';       import Register from './Register';       describe('Register component', () => {         it('renders primary action button with blue background color', () => {           const { getByText } = render(<Register />);           const button = getByText('Register');           expect(button).toHaveStyle('background-color: blue');         });         it('does not affect other buttons across the application', () => {           const { getAllByRole } = render(<Register />);           const buttons = getAllByRole('button');           const primaryButton = buttons.find(button => button.textContent === 'Register');           buttons.forEach(button => {             if (button !== primaryButton) {               expect(button).not.toHaveStyle('background-color: blue');             }           });         });       });     